# Semantic Version Migration Plan

Historical releases use v0.1.x sequencing and do not strictly map to semantic versioning categories.

Recommendation: keep existing v0.1.x archive URLs for compatibility and start applying strict semver categories for future releases.

- MAJOR: breaking/incompatible changes
- MINOR: backwards-compatible new features
- PATCH/FIX: backwards-compatible fixes, archive repairs, docs and maintenance
