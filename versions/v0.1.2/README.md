# Command Word Coach v0.1.2

Archived before the v0.1.3 command-word pack update.

v0.1.2 introduced:

- command-word explainer layout;
- exceptional answer below weak answer;
- `Why it works` inside the exceptional answer box;
- central `versions.json` version history;
- version selector in the header.

The complete v0.1.2 source is recoverable from Git history immediately before the v0.1.3 release. Future releases should include full file copies in each version archive when the connector permits practical bulk file writes.
